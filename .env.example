DATABASE_URL=sqlite:///./sinestra.db
UPLOAD_DIR=./data/books

# Optional. Without it the reader still works and AI answers fall back to
# excerpt-only "mock mode". A key can also be saved from the in-app settings,
# which stores it in the database instead of the environment.
OPENROUTER_API_KEY=
